(define (collect-options)
  (let lp ((mode #:none)
           (arguments (command-line))
           (suffixes '())
           (directories '()))
    (if (null? arguments)
      (values suffixes directories)
      (let ((a (car argument)))
        (cond )
        )
      )
    )
  )
